eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8011/eureka/
server:
  port: 8013
spring:
  application:
    name: service-ribbon
logging:
  level:
    root: debug
  file: ../logs/log.log