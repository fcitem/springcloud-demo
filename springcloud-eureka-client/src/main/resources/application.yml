eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8012/eureka/
server:
  port: 8011
spring:
  application:
    name: service-hi
logging:
  level:
    root: debug
  file: ../logs/log.log